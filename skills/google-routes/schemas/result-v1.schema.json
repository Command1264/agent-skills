{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Command1264/agent-skills/google-routes/result-v1.schema.json",
  "title": "google-routes result envelope v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "cli_contract_version", "profile", "status", "results"],
  "properties": {
    "schema_version": {"const": "1"},
    "cli_contract_version": {"const": "2.0.0"},
    "profile": {"const": "summary"},
    "status": {"enum": ["success", "degraded", "partial_success", "failure"]},
    "results": {
      "type": "array",
      "minItems": 1,
      "items": {
        "oneOf": [
          {"$ref": "#/$defs/route_result"},
          {"$ref": "#/$defs/error_result"}
        ]
      }
    }
  },
  "$defs": {
    "fallback": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["routing_mode", "reason"],
      "properties": {
        "routing_mode": {"type": "string"},
        "reason": {"type": "string"}
      }
    },
    "route_result": {
      "type": "object",
      "additionalProperties": false,
      "required": ["request_id", "status", "travel_mode", "distance_meters", "duration_seconds", "static_duration_seconds", "warnings", "fallback", "origin_place_id", "destination_place_id", "attempts"],
      "properties": {
        "request_id": {"type": "string"},
        "status": {"enum": ["success", "degraded"]},
        "travel_mode": {"enum": ["DRIVE", "TWO_WHEELER"]},
        "distance_meters": {"type": "integer", "minimum": 0},
        "duration_seconds": {"type": "number", "minimum": 0},
        "static_duration_seconds": {"type": "number", "minimum": 0},
        "warnings": {"type": "array", "items": {"type": "string"}},
        "fallback": {"$ref": "#/$defs/fallback"},
        "origin_place_id": {"type": ["string", "null"]},
        "destination_place_id": {"type": ["string", "null"]},
        "attempts": {"type": "integer", "minimum": 1, "maximum": 3}
      }
    },
    "error_result": {
      "type": "object",
      "additionalProperties": false,
      "required": ["request_id", "status", "travel_mode", "error", "attempts"],
      "properties": {
        "request_id": {"type": "string"},
        "status": {"const": "error"},
        "travel_mode": {"enum": ["DRIVE", "TWO_WHEELER"]},
        "attempts": {"type": "integer", "minimum": 1, "maximum": 3},
        "error": {
          "type": "object",
          "additionalProperties": false,
          "required": ["code", "message", "retryable"],
          "properties": {
            "code": {"type": "string"},
            "message": {"type": "string"},
            "retryable": {"type": "boolean"},
            "http_status": {"type": "integer", "minimum": 300, "maximum": 599}
          }
        }
      }
    }
  }
}
